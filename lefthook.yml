pre-commit:
  parallel: true
  commands:
    stylelint:
      glob: "assets/css/*.css"
      run: sh -c 'npx stylelint {staged_files}'
    eslint:
      glob: "assets/js/*.js"
      run: sh -c 'npx eslint {staged_files}'
